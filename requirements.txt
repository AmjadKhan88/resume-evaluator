# Core Flask stack
flask==3.0.3
werkzeug==3.0.3
python-dotenv==1.0.1

# File parsing
PyPDF2==3.0.1
python-docx==1.1.2

# OpenAI SDK (used in app.py for direct Gemini calls if needed)
openai==1.30.1
httpx==0.27.0

# LangChain — do NOT pin langchain-core separately.
# langchain pulls in a compatible langchain-core automatically.
# Pinning langchain-core to an old 0.3.x version while langchain
# needs >=1.4.0 is exactly what caused the conflict.
langchain==1.3.5
langchain-google-genai